directive @requireAuth on FIELD_DEFINITION | ENUM_VALUE

directive @date(
    format: String
    defaultFormat: String = "yyyy-mm-dd HH:mm"
) on FIELD_DEFINITION

directive @paginate(
    defaultPage: Int = 1
    defaultPerPage: Int = 5
) on FIELD_DEFINITION | ENUM_VALUE
