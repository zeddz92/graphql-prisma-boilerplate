directive @requireAuth on FIELD_DEFINITION

directive @date(
    format: String
    defaultFormat: String = "yyyy-mm-dd HH:mm"
) on FIELD_DEFINITION

directive @paginate on FIELD_DEFINITION

directive @length(min: Int, max: Int) on FIELD_DEFINITION | INPUT_FIELD_DEFINITION
